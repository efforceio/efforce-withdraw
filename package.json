{
  "name": "efforce-withdraw",
  "version": "1.0.0",
  "scripts": {
    "webapp-start": "cd webapp && run run start",
    "webapp-build": "cd webapp && run run build",
    "webapp-deploy": "cd webapp && run run deploy",
    "webapp-run-install": "cd webapp && npm install",
    "smartcontract-deploy": "cd webapp && npm run deploy",
    "smartcontract-build": "cd smartcontract && npm run build",
    "smartcontract-release": "cd smartcontract && npm run release",
    "smartcontract-test": "cd smartcontract && npx run test",
    "smartcontract-npm-install": "cd smartcontract && npm install"
  }
}
