{
  "name": "efforce-withdraw",
  "version": "1.0.0",
  "scripts": {
    "webapp-start": "cd webapp && yarn run start",
    "webapp-build": "cd webapp && yarn run build",
    "webapp-deploy": "cd webapp && yarn run deploy",
    "smartcontract-deploy": "cd webapp && yarn run deploy",
    "smartcontract-build": "cd smartcontract && yarn run build",
    "smartcontract-release": "cd smartcontract && yarn run release",
    "smartcontract-test": "cd smartcontract && yarn run test"
  }
}
